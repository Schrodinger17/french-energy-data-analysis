# Load the data

Dataset source : https://www.rte-france.com/eco2mix/telecharger-les-indicateurs

```{r}
# load data.csv file
# row_data <- read.csv("courbe-de-charge-de-la-production-delectricite-par-filiere.csv", header = TRUE, sep = ";")
row_data <- read.csv("eCO2mix_RTE_En-cours-TR.csv", header = TRUE, sep = " ")
attach(row_data)
```

# Data summary
```{r}
# print the data summary
summary(row_data)
```


# Data visualization
```{r}
# convert date_heure column to date format
data$date_heure <- as.Date(data$date_heure, format = "%d/%m/%Y %H:%M")

# visualize dates distribution
hist(data$date_heure, breaks = 100, main = "Distribution des dates", xlab = "Date")

```


# Data visualization
```{r}
# load ggplot2 package
library(ggplot2)

# plot the data
ggplot(data, aes(x = date_heure, y = production_totale_mw, color = Filière)) + geom_line() + geom_point() + labs(title = "Production d'électricité par filière", x = "Année", y = "Production (GWh)")

```